#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C program that prints from 0 to 10 (exclusive)
 *
 * Return: (0) which indicates a success
*/

int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%d", i);

	return (0);
}

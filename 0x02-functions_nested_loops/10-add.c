#include "main.h"

/**
 * add - A function that adds two integers.
 *
 * @a: First integer to get added.
 * @b: Second integer to get added.
 *
 * Return: (a+b) The summed up number from the inputs.
*/

int add(int a, int b)
{
	return (a + b);
}
